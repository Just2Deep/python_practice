# testing a function call

def greet(name, message="Welcome!"):
    print('Hello', name + '.', message)


greet('Candidate')


class Usrdatabase:

    def __init__(self):
        self.users = []

    def insert(self, user):
        i = 0

        while (i < len(self.users)):

            if (self.users.name[i] > self.user.name):
                break
            i += 1

        self.users.insert(i, self.user)

    def update
